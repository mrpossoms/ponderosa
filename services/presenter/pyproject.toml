[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = []
py-modules = ["main"]

[project]
name = "ponderosa-presenter"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "jinja2>=3.1.0",
    "Pillow>=10.0.0",
]
